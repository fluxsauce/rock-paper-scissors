mixin choice(gameId, selection)
  if selection === 'rock'
    span ⛰️
  else if selection === 'paper'
    span 📝
  else if selection === 'scissors'
    span ✂️
  else
    span NOT IMPLEMENTED
